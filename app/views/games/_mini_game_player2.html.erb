  <% if current_user.id  == @player_two.user_id %>

 <div class="mini-game-grid">
  <% @game.player_one_active_cards.each do |card| %>
    <% if card.active == true %>
      <div class="mini-card"></div>
    <% else %>
      <div class="mini-card-false"></div>
    <% end %>
  <% end %>
</div>
<% else %>

 <div class="mini-game-grid">
  <% @game.player_two_active_cards.each do |card| %>
    <% if card.active == true %>
      <div class="mini-card"></div>
    <% else %>
      <div class="mini-card red"></div>
    <% end %>
  <% end %>
</div>
<% end %>
